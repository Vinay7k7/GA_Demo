name: 02 - Workflow Events
# now we will add more then one event triggers 
on:
    push:
    pull_request:
    schedule:
        - cron: '*/5 * * * *'
    workflow_dispatch:
    

jobs:
    echo:
        runs-on: ubuntu-latest
        steps:
            - name: Show the trigger
              run: echo "I have been triggered by a(n) ${{github.event_name}} event."
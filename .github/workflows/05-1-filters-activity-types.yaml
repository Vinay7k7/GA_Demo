name: 05 - 1 - Event FIlter and  Activity Types
on:
    pull_request:
        types: [opened, synchronize]
        branches:
            - main

jobs:
    echo:
        runs-on: ubuntu-latest
        steps:
            - name: Show the trigger
              run: echo "Running when ever the PR is opened and synchronized and base branch branch is main"